<h1>タスク詳細</h1>
  <div>
      <div class="task">
        ジャンル：<%= @task.genre.name %>
        本文：<%= @task.body %>
        期限：<%= @task.due_date %>
        進捗：<%= @task.progress %>
      </div>
  </div>